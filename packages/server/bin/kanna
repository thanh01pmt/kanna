#!/usr/bin/env bun
import process from "node:process"
import { CLI_CHILD_MODE, CLI_CHILD_MODE_ENV_VAR } from "../src/restart"

if (process.env[CLI_CHILD_MODE_ENV_VAR] === CLI_CHILD_MODE) {
  await import("../src/cli.ts")
} else {
  await import("../src/cli-supervisor.ts")
}
